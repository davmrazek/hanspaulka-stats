# raw HTML cache (symlink to storage outside iCloud-synced ~/Documents)
cache
cache/

# SQLite database (symlink, same reason)
data
data/

# iCloud sync conflict copies
* 2.*
* 2/
* 3.*
* 3/

# generated site (keep CNAME if added later)
site/*
!site/CNAME

# python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/

# macOS
.DS_Store

# local logs
*.log

# markdown docs (local planning notes; already-tracked .md files are unaffected)
*.md
